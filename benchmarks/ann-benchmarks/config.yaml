## Deprecated
#
# ann-benchmarks expects algorithm definitions in `config.yml`.
# This file exists only to avoid silent confusion from older references.

deprecated: true
message: "Use config.yml (not config.yaml) for ann-benchmarks algorithm definitions."
