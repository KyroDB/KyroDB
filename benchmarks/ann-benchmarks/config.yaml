## Deprecated
#
# Upstream ann-benchmarks expects algorithm definitions in `config.yml`.
# Keep this file only as a guardrail to prevent accidental use.

raise RuntimeError("Deprecated: use config.yml for ann-benchmarks")
