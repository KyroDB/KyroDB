# Pinned Python dependencies for KyroDB benchmark tooling.
#
# Install:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r benchmarks/requirements.txt
#
# Notes:
# - Keep grpc/protobuf versions in sync with `benchmarks/ann-benchmarks/Dockerfile`.

grpcio==1.60.0
grpcio-tools==1.60.0
protobuf==4.25.3
numpy==1.26.4
h5py==3.10.0
matplotlib==3.8.2
